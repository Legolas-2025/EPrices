# EPrices
ESP32 + ESPHome firmware that fetches day-ahead electricity spot prices from the Energy-Charts API and exposes them as Home Assistant sensors. No token, no subscription. Supports 15-min and hourly resolution, NVS persistence, auto-retry, DST-safe, and any Energy-Charts bidding zone.
